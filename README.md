# 🎥 Cam Analyzer

Интеллектуальная система анализа видеопотока с детекцией людей и уведомлениями в Telegram.

## 🚀 Возможности

- 🔍 **Детекция людей** с использованием YOLOv8
- 📱 **Telegram бот** с командами управления
- 🔔 **Автоматические уведомления** с изображениями при детекции
- ⚙️ **Настраиваемый порог уверенности** для фильтрации ложных срабатываний
- 🏗️ **Микросервисная архитектура** - отдельные сервисы для бота и анализатора
- 🐳 **Docker контейнеризация** для простого развертывания
- 📊 **Структурированное логирование** для мониторинга

## 🛠️ Технологический стек

- **Python 3.12** - основной язык
- **OpenCV** - обработка видео
- **YOLOv8** - детекция объектов
- **aiogram** - Telegram Bot API
- **Pydantic** - валидация конфигурации
- **Docker** - контейнеризация
- **uv** - управление зависимостями

## 📋 Требования

- Docker и Docker Compose
- Или Python 3.12+ с uv для локальной разработки

## 🚀 Быстрый старт

### Локальная разработка (рекомендуется)

⚠️ **Рекомендуется использовать локальный запуск** вместо Docker из-за:
- 🐌 Очень долгой сборки Docker образа (20+ минут)
- 💾 Большого размера образа с OpenCV зависимостями
- 🔥 Высокого потребления ресурсов

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd cam-analyser
```

2. Установите зависимости:
```bash
make install
# или
uv sync
```

3. Создайте `.env` файл:
```bash
# Пример конфигурации
MODEL=yolov8s.pt
CAMERA_URL=http://192.168.1.100:8080
CONFIDENCE_THRESHOLD=0.85
CHECK_INTERVAL=5
BOT_TOKEN=your_telegram_bot_token
MESSAGE_CHAT_IDS=[your_chat_id]
```

4. Запустите сервисы в разных терминалах:

**Терминал 1: Запуск Telegram бота**
```bash
make run-bot
# или
uv run python -m src.bot.main
```

**Терминал 2: Запуск анализатора камеры**
```bash
make run
# или  
uv run python -m src.main
```

⚠️ **Важно:** Оба сервиса должны работать одновременно:
- 🤖 **Бот** - для обработки команд пользователей
- 📷 **Анализатор** - для детекции и отправки уведомлений

### Docker (не рекомендуется для разработки)

⚠️ **Внимание:** Docker запуск имеет серьезные проблемы производительности:
- 🐌 Сборка образа занимает 20+ минут
- 💾 Очень большой размер итогового образа  
- 🔥 Высокое потребление ресурсов системы
- 🐛 Сложности с отладкой в контейнере

**Использовать только для продакшена** после оптимизации образов.

1. Создайте `.env` файл:
```bash
# Те же переменные что и для локального запуска
MODEL=yolov8s.pt
CAMERA_URL=http://192.168.1.100:8080
CONFIDENCE_THRESHOLD=0.85
CHECK_INTERVAL=5
BOT_TOKEN=your_telegram_bot_token
MESSAGE_CHAT_IDS=[your_chat_id]
```

2. Запустите приложение:
```bash
make app
# или
docker compose up -d
```

3. Проверьте логи:
```bash
docker compose logs -f
```

## 📖 Использование

### Make команды

```bash
# Docker команды
make app           # Запуск в Docker
make app-down      # Остановка контейнеров
make app-build     # Пересборка образа
make app-restart   # Перезапуск
make app-logs      # Просмотр логов
make app-shell     # Подключение к контейнеру

# Локальная разработка
make install       # Установка зависимостей
make run           # Запуск анализатора камеры
make run-bot       # Запуск Telegram бота
# Утилиты
make clean         # Очистка Docker ресурсов
make build-size    # Показать размеры образов
```

### Telegram бот команды

После запуска бота, доступны следующие команды:

- `/start` - Информация о системе и доступных командах
- `/status` - Проверка статуса системы
- `/help` - Справка по всем командам

**Важно:** Для получения уведомлений ваш chat_id должен быть указан в переменной `MESSAGE_CHAT_IDS`.

### Пример работы системы

**Сценарий работы:**

1. **📷 Анализ видеопотока** - система непрерывно получает кадры с IP-камеры
2. **🤖 YOLO детекция** - каждый кадр обрабатывается моделью YOLOv8 для поиска людей
3. **⚖️ Проверка порога** - если уверенность детекции ≥ заданного порога (например, 0.85)
4. **🖼️ Аннотация изображения** - на кадр наносится красная рамка вокруг обнаруженного человека
5. **📱 Отправка уведомления** - аннотированное изображение отправляется в Telegram

**Пример уведомления в Telegram:**

```
🚨 Обнаружен человек!
📅 Дата: 07.09.2025
🕐 Время: 17:49:56
🎯 Уверенность: 0.81

[Изображение с красной рамкой вокруг человека]
Файл: detection_person_0.81_20250907_174956_123.jpg
```

**Технические детали:**
- **Формат изображения:** JPG с качеством сжатия
- **Имя файла:** `detection_person_{confidence}_{timestamp}.jpg`
- **Аннотации:** Красные прямоугольники с подписями класса и уверенности
- **Частота проверки:** Настраиваемая через `CHECK_INTERVAL` (секунды)
- **Порог срабатывания:** Настраиваемый через `CONFIDENCE_THRESHOLD` (0.0-1.0)

### Конфигурация

| Переменная | Описание | Пример |
|------------|----------|---------|
| `MODEL` | Путь к YOLO модели(приммер если модель в корне проекта) | `yolov8s.pt` |
| `CAMERA_URL` | HTTP URL камеры | `http://192.168.1.100/` |
| `CONFIDENCE_THRESHOLD` | Порог уверенности (0.0-1.0) | `0.9` |
| `CHECK_INTERVAL` | Интервал проверки (секунды) | `5` |
| `BOT_TOKEN` | Токен Telegram бота | `123456:ABC-DEF...` |
| `MESSAGE_CHAT_IDS` | ID телеграм чатов для уведомлений, в боте нужно нажать старт, чтобы фотографии корректно отправлялись | `[123456789]` |

## 🏗️ Архитектура

### Микросервисная архитектура

Система состоит из двух независимых сервисов:

1. **🤖 Telegram Bot Service** (`src/bot/main.py`)
   - Обрабатывает команды пользователей
   - Предоставляет интерфейс управления
   - Работает независимо от анализатора

2. **📷 Camera Analyzer Service** (`src/main.py`)
   - Анализирует видеопоток камеры
   - Детектирует людей с помощью YOLO
   - Отправляет уведомления через Telegram API

### Компоненты системы

Проект следует принципам **SOLID** и использует **Dependency Injection**:

- **🔌 Interfaces** - Протоколы для всех компонентов
- **📷 CameraService** - Работа с камерой
- **🤖 YOLODetectorService** - Детекция объектов  
- **🖼️ ImageProcessor** - Обработка изображений
- **📨 AlertService** - Отправка уведомлений
- **🎯 CamAnalyzer** - Главный координатор
- **💉 DIContainer** - Контейнер зависимостей
- **🔧 TelegramService** - Интеграция с Telegram API

## 📊 Логирование

Приложение использует структурированное JSON логирование:

```json
{
  "action": "person_detected",
  "status": "success", 
  "data": {
    "confidence": 0.85,
    "timestamp": "20241201_143022_123"
  }
}
```

## 🔧 Разработка

### Структура проекта

```
src/
├── main.py                 # 📷 Точка входа анализатора камеры
├── config.py              # ⚙️ Общая конфигурация
├── locales.py             # 🌐 Локализация сообщений
├── detector/              # 🔍 Модуль детекции
│   ├── __init__.py        
│   ├── interfaces.py      # 🔌 Протоколы и интерфейсы
│   ├── camera_service.py  # 📷 Сервис работы с камерой
│   ├── yolo_service.py    # 🤖 YOLO детектор объектов
│   ├── image_processor.py # 🖼️ Обработка изображений
│   ├── alert_service.py   # 📨 Сервис уведомлений
│   ├── cam_analyzer.py    # 🎯 Главный анализатор
│   └── enums.py           # 📝 Перечисления
└── bot/                   # 🤖 Telegram бот
    ├── main.py            # 🚀 Точка входа бота
    ├── config.py          # ⚙️ Конфигурация бота
    └── service.py         # 🔧 Сервис бота
```

### Docker структура

```
├── Dockerfile             # 🐳 Общий образ для обоих сервисов
├── docker-compose.yml     # 🔧 Конфигурация микросервисов
├── .env                   # 🔐 Переменные окружения
└── yolov8s.pt            # 🧠 Модель YOLO
```

## 🚦 Мониторинг и отладка

### Просмотр логов Docker

```bash
# Логи всех сервисов
docker compose logs -f

# Логи конкретного сервиса
docker logs cam-analyser-bot -f
docker logs cam-analyser-detector -f
```

### Проверка статуса

```bash
# Статус контейнеров
docker compose ps

# Использование ресурсов
docker stats

# Проверка размера образов
make build-size
```

## 📋 TODO / Планы развития

### 🐳 Оптимизация Docker

**Приоритет: Высокий**

Текущие проблемы:
- ⏰ Время сборки образа: **20+ минут**
- 💾 Размер образа: **очень большой** из-за всех OpenCV зависимостей
- 🔄 Повторная сборка при изменении кода занимает много времени

Планы оптимизации:
- [ ] **Multi-stage build** - отдельные стадии для dependencies и application
- [ ] **Кэширование слоев** - оптимизация порядка COPY команд
- [ ] **Минимальные зависимости** - установка только необходимых OpenCV библиотек
- [ ] **Альтернативный базовый образ** - использование `python:3.12-alpine` вместо `python:3.12-slim`
- [ ] **Отдельные образы** - разные Dockerfile для бота (легкий) и анализатора (с OpenCV)
- [ ] **.dockerignore** - исключение ненужных файлов из контекста сборки

### 🚀 Архитектурные улучшения

- [ ] **RabbitMQ интеграция** - переход на асинхронную архитектуру:
  - 📸 **Producer** - анализатор камеры отправляет изображения в очередь RabbitMQ
  - 📱 **Consumer** - отдельный сервис обрабатывает очередь и отправляет в Telegram
  - ⚡ **Преимущества:** горизонтальное масштабирование, отказоустойчивость, буферизация
  - 🔄 **Очереди:** `detection.images`, `notifications.telegram`

### 🚀 Другие улучшения

- [ ] **Healthcheck** для контейнеров
- [ ] **Graceful shutdown** при остановке сервисов
- [ ] **Метрики и мониторинг** (Prometheus/Grafana)
- [ ] **Конфигурация через ConfigMap** для Kubernetes
- [ ] **Автотесты** и CI/CD pipeline
- [ ] **Поддержка RTSP** потоков камер
- [ ] **Web интерфейс** для управления системой
